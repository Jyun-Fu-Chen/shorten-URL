<div class="d-flex flex-column align-items-center mt-5">
  <div class="">
    <h4>Success!!Please use this link:</h4>
  </div>
  <div class="mt-2">
     <a class="ml-3" id="Url" href="{{#if onLine}}localhost:{{/if}}{{PORT}}/{{shortUrl}}">
      <h3>{{#if onLine}}localhost:{{/if}}{{PORT}}/{{shortUrl}}</h3>
    </a>
  </div>
  <div class="mt-5">
    <button id="copyBtn" class="btn btn-primary" data-clipboard-target="#Url">Copy</button>
  </div>
</div>